My personal pi package.

Contains extensions for functionality and themes.

As well as some custom skills. Also serves as a monorepo to pull in third party ones.

Use ./dev.sh to launch pi using the local version (it uses --no-extensions and adds the extensions manually).
